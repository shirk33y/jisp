Sidebar:
  {
    [items: [], config]:
      {
        div,
        rounded-2xl: {config: {site, rounded-borders}},
        px-5: {gt, 0, {list: {foo: {{len: .}}}, items}},
        mx-4,
        lg:mx-2,
        {ul, mx-15, [each: {items, item, i}, [item, authors, i, name]]},
        {
          ul,
          border-foo,
          bg-main-400,
          justify-end,
          items-center: {eq, {., item, "url"}, "/home"},
          lg:500,
          {
            each,
            {items, item, index}:
              {
                li,
                rounded-full: [eq, 0, [mod, index, 2]],
                lg:border-500,
                bg-blue-400,
                {text, [., item, "authors", index]},
              },
          },
          each,
          {items, item, index}:
            {
              li,
              rounded-full: [eq, 0, [mod, index, 2]],
              lg:border-500,
              bg-blue-400,
              {text, [., item, "authors", index]},
            },
        },
        {header, rounded-123},
      },
  }
