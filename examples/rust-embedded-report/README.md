# Rust embedded report

This is the downstream-style `jisp_macros::lisp_file!` example: two Jisp
modules, a named report result, callbacks, and an error path. `main` returns
`42` in the macro integration test and the interpreter.
